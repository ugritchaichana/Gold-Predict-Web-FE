# Gold Predict Web - Environment Variables Example
# ====================================================
# Copy this file to .env and update the values as needed
# 
# Environment Files Loading Priority:
# 1. .env.[mode].local (highest priority, git-ignored)
# 2. .env.local (git-ignored)
# 3. .env.[mode] (.env.development or .env.production)
# 4. .env (default fallback)
#
# Usage:
# - Development: pnpm dev (loads .env.development + .env)
# - Production:  pnpm build (loads .env.production + .env)

# ====================================================
# API Configuration
# ====================================================

# API Base URL
# This is the main backend API endpoint
# For development: http://127.0.0.1:8000 (local backend)
# For production: https://gold-predictions.duckdns.org (live server)
VITE_API_BASE_URL=https://gold-predictions.duckdns.org

# ====================================================
# Environment Settings
# ====================================================

# Environment Type
# Used by the application to determine behavior
# Options: development, production
# - development: enables console logs, debug info, dev tools
# - production: disables logs, optimized for performance
VITE_ENVIRONMENT=production

# ====================================================
# Additional Configuration (Optional)
# ====================================================

# Application Info (uncomment if needed)
# VITE_APP_NAME=Gold Predict Web
# VITE_APP_VERSION=1.0.0

# Feature Flags (uncomment if needed)
# VITE_ENABLE_DEBUG_MODE=false
# VITE_ENABLE_CACHE_DEBUG=false

# ====================================================
# Notes:
# ====================================================
# - All environment variables must start with VITE_ to be accessible in the browser
# - Changes to .env files require a restart of the dev server
# - Never commit .env files with sensitive data to git
# - Use .env.example for documentation and team reference